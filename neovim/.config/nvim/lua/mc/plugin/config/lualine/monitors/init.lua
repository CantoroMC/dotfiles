return {
  nr_lines    = require((...) .. '.buffer').nr_lines,
  file_size   = require((...) .. '.buffer').file_size,
  vista       = require((...) .. '.vista').vista,
  whitespaces = require((...) .. '.whitespaces').whitespaces,
}

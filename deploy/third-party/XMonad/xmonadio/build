#!/bin/sh

SRC_DIR=$XMONAD_CONFIG_DIR
EXE_NAME=xmonad-x86_64-linux
output_file=$1; shift

cd $SRC_DIR

cabal new-build

find dist-newstyle -type f -executable -name $EXE_NAME \
  -exec mv -u '{}' $output_file ';'
